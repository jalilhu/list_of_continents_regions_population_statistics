In order for the code to run as intended, please run main.py.

If you run this for the first time, please make sure that the packages in requirements.txt are installed.

To do so, run this command:
	pip install -r requirements.txt